def perimeter(length, breadth) { 2 * (length + breadth)}

def area(length, breadth) { length * breadth }


// Internally
/* class X {

    X() {}

    def perimeter(length, breadth) { 2 * (length + breadth)}

    def area(length, breadth) { length * breadth }
} */